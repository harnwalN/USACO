#include <bits/stdc++.h>
using namespace std;

int main() {
    int n;
    cin >> n;
    vector<pair<int, int>> i_s;
    vector<int> a_sorted;

    for (int i = 0; i<n; i++) {
        int val;
        i_s.push_back(make_pair(i, val));
        a_sorted.push_back(val);
    }
}